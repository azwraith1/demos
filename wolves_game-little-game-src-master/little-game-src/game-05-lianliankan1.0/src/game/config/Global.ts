/**
  * 全局数据存储
  */
module Global {

  export var audioProxy: game.AudioProxy;

  export var playerProxy: game.PlayerProxy;

  export var netProxy: game.NetProxy;

  export var gameProxy: game.GameProxy;

  export var otherPlayerProxy: game.OtherPlayerProxy;

  export var tikiSocketProxy: game.TikiSocketProxy;

  export var tikiHttpProxy: game.TikiHttpProxy;
}